[package]
name = "sovoxd-stub"
version = "0.0.1"
edition = "2024"
rust-version = "1.85" # Arch §3 toolchain floor
license = "Apache-2.0"
description = "Sovox health/version stub daemon — the seed of the sovoxd health supervisor (Arch §4.5)"

# Deliberately dependency-free (std only): the handwritten Cargo.lock stays
# trivial and buildRustPackage needs no network or vendored hashes.
[dependencies]
